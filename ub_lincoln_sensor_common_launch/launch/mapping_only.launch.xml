<launch>
    <!-- Config-->
  <arg name="launch_driver" default="true" description="do launch driver"/>  
  <!-- LIDAR Config-->
  <arg name="vehicle_mirror_param_file" default="$(find-pkg-share ub_lincoln_vehicle_description)/config/mirror.param.yaml"/>
  <arg name="output_file_name" description="Please provide the name of the bag you are collecting along with path if no path bag will be saved in WORKINGDIR"/>
  <arg name="topics_to_record" description="Please provide the topics you want to record or give -a to record all published topics."/>
  <!-- Robot State Publisher -->
  <arg name="vehicle_model" default="ub_lincoln_vehicle" description="vehicle model name"/>
  <arg name="sensor_model" default="ub_lincoln_sensor_kit" description="sensor model name"/>
  <arg name="config_dir" default="$(find-pkg-share $(var sensor_model)_description)/config" description="path to dir where sensors_calibration.yaml, etc. are located"/>
  <arg name="vehicle_info_file" default="$(find-pkg-share $(var vehicle_model)_description)/config/vehicle_info.param.yaml" description="path to vehicle_info.yaml file"/>
  
  <!-- Robot State Publisher for TF's -->
  <group>
    <arg name="model_file" default="$(find-pkg-share autoware_core_vehicle)/urdf/vehicle.xacro" description="path to the file of model settings (*.xacro)"/>
    <node name="robot_state_publisher" pkg="robot_state_publisher" exec="robot_state_publisher">
      <param name="robot_description" value="$(command 'xacro $(var model_file) vehicle_model:=$(var vehicle_model) sensor_model:=$(var sensor_model) config_dir:=$(var config_dir)' 'warn')"/>
    </node>
  </group>

  <!-- Sensing -->
  <group>
    <push-ros-namespace namespace="sensing"/>

    <!-- LIDAR -->
    <group>
      <let name="driver_pointcloud_topic_name" value="/velodyne_points"/>
      <push-ros-namespace namespace="lidar"/>
      <group>
        <push-ros-namespace namespace="top"/>
        <include file="$(find-pkg-share ub_lincoln_sensor_common_launch)/launch/nebula_node_container.launch.py">
          <arg name="sensor_model" value="VLP32"/>
          <arg name="launch_driver" value="$(var launch_driver)"/>
          <arg name="config_file" value="$(find-pkg-share ub_lincoln_sensor_common_launch)/config/VLP32.param.yaml"/>
          <arg name="launch_driver_only" value="true"/>
          <arg name="vehicle_mirror_param_file" value="$(var vehicle_mirror_param_file)"/>
          <arg name="container_name" value="pointcloud_container"/>
        </include>
      </group>
    </group>

    <!-- GNSS -->
    <group>
      <let name="navsatfix_topic_name" value="/sensing/gnss/novatel/oem7/fix"/>
      <push-ros-namespace namespace="gnss"/>
      <group>
        <set_env name="NOVATEL_OEM7_DRIVER_PARAM_OVERRIDES_PATH" value="$(find-pkg-share ub_lincoln_sensor_common_launch)/config/novatel_overrides_std_msg_topics.yaml"/>
        <include file="$(find-pkg-share novatel_oem7_driver)/launch/oem7_net.launch.py">
          <arg name="oem7_ip_addr" value="192.168.100.201"/>
          <arg name="oem7_port" value="3005"/>
        </include>
      </group>
    </group>
  </group>

  <!-- Ros Bag Record -->
  <executable cmd="ros2 bag record $(var topics_to_record) -o $(var output_file_name)"/>
  
</launch>
